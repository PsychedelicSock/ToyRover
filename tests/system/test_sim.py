def test_rover_commands():
    assert False


def test_move_oob():
    # Start fine, prevent a move into oob, then move away successfully
    assert False


def test_place_oob():
    # Start fine, try to place oob, then move away successfully
    assert False


def test_start_oob():
    # Start oob, try to move but this does nothing, then place successfully and move away.
    assert False
