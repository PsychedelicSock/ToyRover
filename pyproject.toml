[project]
name = "toyrover"
version = "0.1.0"
description = "Toy Rover demo project"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [

]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project.scripts]
main = "toyrover.run:run"
